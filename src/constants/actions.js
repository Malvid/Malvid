'use strict'

const SET_ROUTE = 'set_route'
const SET_CURRENT_SIZE_STATUS = 'set_current_size_status'
const SET_CURRENT_SIZE_VERTICAL = 'set_current_size_vertical'
const SET_CURRENT_SIZE_HORIZONTAL = 'set_current_size_horizontal'
const HYDRATE = 'hydrate'

module.exports = {
	SET_ROUTE,
	SET_CURRENT_SIZE_STATUS,
	SET_CURRENT_SIZE_VERTICAL,
	SET_CURRENT_SIZE_HORIZONTAL,
	HYDRATE
}
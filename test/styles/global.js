'use strict'

const assert = require('chai').assert
const global = require('../../src/styles/global')

describe('global', function() {

	it('should be a string', function() {

		assert.isString(global)

	})

})
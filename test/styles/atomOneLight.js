'use strict'

const assert = require('chai').assert
const atomOneLight = require('../../src/styles/atomOneLight')

describe('atomOneLight', function() {

	it('should be a string', function() {

		assert.isString(atomOneLight)

	})

})
'use strict'

const assert = require('chai').assert
const navItem = require('../../src/styles/navItem')

describe('navItem', function() {

	it('should be an object', function() {

		assert.isObject(navItem)

	})

})
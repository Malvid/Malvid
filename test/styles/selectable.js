'use strict'

const assert = require('chai').assert
const selectable = require('../../src/styles/selectable')

describe('selectable', function() {

	it('should be an object', function() {

		assert.isObject(selectable)

	})

})
'use strict'

const assert = require('chai').assert
const normalize = require('../../src/styles/normalize')

describe('normalize', function() {

	it('should be a string', function() {

		assert.isString(normalize)

	})

})
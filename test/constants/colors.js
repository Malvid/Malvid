'use strict'

const assert = require('chai').assert
const colors = require('../../src/constants/colors')

describe('colors', function() {

	it('should be an object', function() {

		assert.isObject(colors)

	})

})
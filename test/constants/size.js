'use strict'

const assert = require('chai').assert
const size = require('../../src/constants/size')

describe('size', function() {

	it('should be an object', function() {

		assert.isObject(size)

	})

})
'use strict'

const assert = require('chai').assert
const currentSize = require('../../src/constants/currentSize')

describe('currentSize', function() {

	it('should be an object', function() {

		assert.isObject(currentSize)

	})

})
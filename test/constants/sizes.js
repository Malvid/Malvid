'use strict'

const assert = require('chai').assert
const sizes = require('../../src/constants/sizes')

describe('sizes', function() {

	it('should be an object', function() {

		assert.isObject(sizes)

	})

})